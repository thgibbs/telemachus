{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Narrow capabilities for the main Telemachus window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
